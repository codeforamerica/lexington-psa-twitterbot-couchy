<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">

  <title>Save Couchy!</title>
  <style>
    .container {
      margin: 0 auto;
      width: 50%;
      position: relative;
    }
    .tweet {
      position: absolute;
      top: 400;
    }
    .couch {
      top: 0;
      left: 0;
      position: absolute;
    }
    .avatar-container {
      width: 48px;
    }
    .avatar-container img {
      position: relative;
      width: 100%;
    }
    .avatar {
      top: 186;
      left: 422;
      position: absolute;
      max-width: 100%;
    }
    @media (max-width: 800px) {
      .container {
        width: 100%;
      }
      .couch {
        width: 300px;
      }
      .avatar-container {
        width: 34px;
      }
      .avatar {
        top: 87px;
        left: 205px;
      }
    }
  </style>
  <script src="//code.jquery.com/jquery-1.11.0.min.js"></script>
  <script>
    $(function() {
      var interval = 10000;

      function updateTweets() {
        $.get('/tweets', function(tweets) {
          displayTweets(tweets);
        });
      }

      function displayTweets(tweets) {
        var tweet = tweets[0];
        $('.tweet').html(tweets[0].embedded.html);
        $('.avatar').prop('src', tweet.user.profile_image_url);
        // tweetHtml = '';
        // tweets.forEach(function(tweet) {
        //   // tweetHtml += '<li><img src="' + tweet.user.profile_image_url + '" xheight="100" xwidth="100">' + tweet.text + '</li>';
        //   tweetHtml += '<li><blockquote class="twitter-tweet" width="600">' + tweet.embedded.html + '</blockquote></li>';
        // });
        // $("ul.tweets").html(tweetHtml);
      };

      setInterval(updateTweets, interval);
    });
  </script>
  <script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-47565520-3', 'savecouchy.com');
    ga('send', 'pageview');

  </script>
  </head>
  <body>
    <div class="container">
      <div class="avatar-container">
        <img class="avatar" src="https://pbs.twimg.com/profile_images/821409818/erik_02_square_bigger.jpg">
      </div>
      <img class="couch" src="/public/BBNGif.gif">
    </div>
    <div class="container">
      <div class="tweet"></div>
      <!-- <ul class="tweets"> -->
      <!--   <% latestTweets.forEach(function(tweet) { %> -->
      <!--     <li>fooo</li> -->
      <!--   <% }) %> -->
      <!-- </ul> -->
    </div>
  </body>
</html>
